package com.github.rlishtaba.commutable

import org.scalatest._

import scala.collection.mutable.{Map => params}

class MockSpec extends FlatSpec with Matchers {
//  it should "return expected type" in {
//    val adapter = new Mock(params("dataIn" -> "hello"))
//    adapter shouldBe a[Mock]
//  }
//
//  it should "return expected byte array" in {
//    val expectedPayload = Array(104, 101, 108, 108, 111)
//    val adapter = new Mock(params("dataIn" -> "hello"))
//    adapter.recv(5) shouldBe expectedPayload
//  }
//
//  it should "return expected exception" in {
//    val adapter = new Mock(params("dataIn" -> "hello"))
//    an[adapter.OutOfMockDataError] should be thrownBy adapter.recv(6)
//  }
}
